@{
    ViewBag.Title = "SignUp";
}
@model FriendsApp.ViewModels.SignUpViewModel

<link href="~/Content/signup.css" rel="stylesheet" />

@Html.ValidationSummary(excludePropertyErrors: true)
@using (Html.BeginForm("SignUp", "Account", FormMethod.Post, new {role = "form", id="signup-form"}))
{
    <h2 class="text-center">Create Account</h2>
    @Html.ValidationMessageFor(m => m.Email)
    @Html.TextBoxFor(m => m.Email, new { placeholder = "Email", autofocus = true, @class = "form-control" })
    @Html.ValidationMessageFor(m => m.Password)
    @Html.PasswordFor(m => m.Password, new { placeholder = "Password", @class = "form-control" })
    <input type="submit" class="btn btn-lg btn-primary" value="Submit" />
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}